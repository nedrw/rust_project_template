///! Library module - exports core functionality for integration tests and external use

// Core modules (always available)
pub mod error;
pub mod prelude;

// Optional modules container
pub mod optional;

// Re-export commonly used types for convenience
pub use error::Error;
pub use prelude::Result;
